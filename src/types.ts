// Declare scheme
export enum Scheme {
    VISMAIDENTITY = "visma-identity"
}

// Declare all valid paths.
export enum Path {
    LOGIN = 'login',
    CONFIRM = 'confirm',
    SIGN = 'sign'
}

// Declare all valid URI Parameters.
export enum URIParameter {
    SOURCE = 'source',
    PAYMENTNUMBER = 'paymentnumber',
    DOCUMENTID = 'documentid'
}
